# 环境依赖参考清单（conda env：`spark`）

> **用途**：本文是 SpaceFin Agent **完整环境依赖的参考清单（reference）**，列出复现开发环境所需的全部组件、版本、获取来源与安装位置。
> **按需接入（Just-in-Time）**：遵循仓库铁律「循序渐进，按需接入」——**不要一次性全装**。下表按「接入时机」分层标注：标「现在·核心」的现在就装（跑通 PoC / 提交规范所需），其余标明了对应阶段（L0/L2/L3），**等真实开发用到该层时再下载**，本文即其获取参考。
> **环境名变更**：原参考配置 `spark42` 已统一改为 **`spark`**。
> **单一宿主环境**：全仓库宿主侧只用 conda env `spark`（Python 3.10.18）一个环境——ETL / 编排 / 测试 / 宿主渲染服务 / pre-commit 钩子 / PySpark 产品栈都在其中。`tools/orchestrator/.venv` 是指向该环境的**符号链接别名**（脚本默认路径用 `.venv/bin/python` 即可，解析到 spark），**不是独立环境**。采集/ETL 运行依赖清单见 `tools/orchestrator/requirements.txt`（已装入 spark）。
> **本机宿主事实（Linux，`/home/azureuser`）**：conda 为 **Miniforge**，位于 `~/miniforge`，唯一环境 `spark`（Python 3.10.18）；`conda` 不在默认 PATH，操作前先 `conda activate spark` 或 `source ~/miniforge/etc/profile.d/conda.sh`。Node v24 位于 `~/node24/bin`（含 npm/npx），同样不在默认 PATH。Docker 需 `sudo`——azureuser 虽在 docker group，但非登录 shell 不会重读组变更，`docker ps` 前先 `newgrp docker` 或直接加 `sudo`。
> **依据**：桌面参考文档《spark42_环境版本汇总.md》＋ 本仓库 `environment.yml` / `requirements.txt` / `requirements-lite.txt`。

---

## 0. 两层依赖与最小可运行集

环境依赖分两层：

| 层 | 内容 | 安装位置 |
|----|------|---------|
| Conda 环境 `spark` | Python 生态（PySpark / aiohttp / selenium / pre-commit …） | conda env（虚拟，不污染系统） |
| 系统级二进制 | Hadoop / Hive / Spark / MySQL / Java | 操作系统（独立安装，不进 conda） |

**最小可运行集（现在就装，即可跑通现有 PoC 与提交规范）：**
`conda` + `Python 3.10.18` + `OpenJDK 17` + 爬虫三件套（`aiohttp` / `beautifulsoup4` / `selenium`）+ `pre-commit` + `Node v24`（commitlint）。
→ 对应 `requirements-lite.txt`，下载量很小。

**阶段性依赖（用到对应能力层时再下）：**
`PySpark`（L2/L3 真实 Sedona/AVM）、`Hadoop`/`Hive`/`Spark`（L0/L2 真实湖仓与空间计算）、`MySQL`（业务源库，Sprint 0 已用 Docker 可暂不下）、`pytest`（阶段 5 单测）、`Chrome`（Selenium 爬虫）等。

---

## 1. 完整依赖清单（总览，含接入时机）

| # | 组件 | 版本 | 接入时机（按需） | 获取方式 / 参考来源 | 装到哪 |
|---|------|------|----------------|-------------------|--------|
| 1 | Miniforge | latest | 现在·核心（本机已装于 `~/miniforge`） | [官网安装器](https://github.com/conda-forge/miniforge) | `~/miniforge`（提供 `conda`） |
| 2 | Python | 3.10.18 | 现在·核心 | conda | env `spark` |
| 3 | OpenJDK | 17 | 现在·核心（PySpark 运行依赖） | conda `openjdk=17` 或 [Adoptium](https://adoptium.net/) | env `spark` / 系统 |
| 4 | PySpark | 4.2.0 | **L3 AVM / L2 真实 Sedona**（纯 PoC 可省） | pip（env 内） | env `spark` |
| 5 | aiohttp | latest | 现在·爬虫 | pip | env `spark` |
| 6 | beautifulsoup4 | latest | 现在·爬虫 | pip | env `spark` |
| 7 | selenium | latest | 用到 Selenium 爬虫再下 | pip | env `spark` |
| 8 | pre-commit | 4.6.1 | 现在·提交规范 | pip | env `spark` |
| 9 | pytest | latest | 阶段 5 单测（用到再下） | pip | env `spark` |
| 10 | Hadoop | 3.3.2 | **L0 湖仓**（真实接入再下） | [官方二进制](https://hadoop.apache.org/releases.html) | 系统 |
| 11 | Hive | 4.0.1 | **L0 湖仓** | [官方二进制](https://hive.apache.org/downloads.html) | 系统 |
| 12 | Spark | 4.2.0（for Hadoop 3） | **L2/L3 真实计算** | [官方二进制](https://spark.apache.org/downloads.html) | 系统 |
| 13 | MySQL | 8.4.10 | L0 业务源库（Sprint 0 已用 Docker，可暂不下） | [官方](https://dev.mysql.com/downloads/) 或 Docker | 系统 / 容器 |
| 14 | Node.js + npm | v24.16.0 | 现在·提交规范（commitlint） | [官网](https://nodejs.org/) 或 nvm | `~/node24/bin`（本机已装） |
| 15 | @commitlint/cli + config-conventional | 19.8.1 | 现在·提交规范 | npm（项目内 `npm install`） | 系统 / 项目 |
| 16 | Google Chrome / Chromium | latest | 用到 Selenium 爬虫再下 | [Chrome](https://www.google.com/chrome/) / [Chromium](https://www.chromium.org/) | 系统 |

> 标「现在·核心 / 现在·爬虫 / 现在·提交规范」的，建议首次搭建就装；标「L0 / L2 / L3 / 用到再下」的，等开发推进到对应能力层时，按本文给出的来源下载即可，无需提前准备。

---

## 2. 方式一：conda 一键建环境（推荐，含现在就装的全部）

前提：本机已装 Miniforge（`conda` 位于 `~/miniforge/bin`，不在默认 PATH，先用 `source ~/miniforge/etc/profile.d/conda.sh` 或 `conda activate spark`）。`environment.yml` 已钉版：python 3.10.18 + openjdk 17 + pyspark 4.2.0 + 爬虫/工具链。

```bash
conda env create -f environment.yml
conda activate spark
python -c "import pyspark; print('pyspark', pyspark.__version__)"   # 期望 4.2.0
java -version                                                      # 期望 17.x
```

> 若 `pyspark==4.2.0` 在 PyPI 尚未发布，把 `environment.yml` 中该行改为 `pyspark` 取最新。
> 本机（Linux 宿主）已装 Miniforge 于 `~/miniforge`，无需重装；若全新机器：`curl -fsSL https://github.com/conda-forge/miniforge/releases/latest/download/Miniforge3-Linux-x86_64.sh -o miniforge.sh && bash miniforge.sh -b -p $HOME/miniforge`，装后 `source ~/miniforge/etc/profile.d/conda.sh`。

## 3. 方式二：venv + pip（不使用 conda，仅无 conda 机器的替代）

前提：本机有 Python 3.10 与 Java 17。

```bash
python3.10 -m venv tools/orchestrator/.venv   # 与 conda spark 保持同一路径约定（别名位）
source tools/orchestrator/.venv/bin/activate  # Windows: tools\orchestrator\.venv\Scripts\activate
pip install -r tools/orchestrator/requirements.txt   # 采集/ETL 依赖
pip install -r requirements.txt               # 产品栈（含 pyspark 4.2.0 + 爬虫/工具链）
```

> 无论走 conda 还是 venv，**宿主只保留一个 Python 环境**；`tools/orchestrator/.venv` 在本机是指向 conda `spark` 的符号链接，无 conda 的机器则在此建真实 venv。

## 4. 轻量版（只跑 PoC，跳过 PySpark / 重型栈）

若当前只想验证概念，用 `requirements-lite.txt`（无 PySpark，下载量极小）：

```bash
pip install -r requirements-lite.txt
python docs/poc/data-foundation/avm_poc.py      # 零依赖，无需安装也能跑
python docs/poc/core-prototype/mvp_prototype.py  # 零依赖，无需安装也能跑
```

## 5. 系统级二进制（不进 conda，独立安装，按层接入）

| 组件 | 版本 | 接入层 | 官方获取 |
|------|------|--------|---------|
| Java OpenJDK | 17.0.19 | 现在·核心 | [Adoptium](https://adoptium.net/)（或 env 内 `conda install openjdk=17`） |
| Hadoop | 3.3.2 | L0 湖仓 | https://hadoop.apache.org/releases.html |
| Hive | 4.0.1 | L0 湖仓 | https://hive.apache.org/downloads.html |
| Spark | 4.2.0（for Hadoop 3，构建含 `-Phive -Phive-thriftserver`） | L2/L3 | https://spark.apache.org/downloads.html |
| MySQL | 8.4.10 | L0（Sprint 0 已用 Docker） | https://dev.mysql.com/downloads/ |

> PySpark 4.2.0 自带 bundled Hadoop 3.x 客户端，本地独立 Hadoop 3.3.2 多数场景兼容。运行 PySpark 前确保 `JAVA_HOME` 指向 Java 17。

## 6. 提交规范工具链（Node / commitlint）

`environment.yml` 不含 Node。本机（Linux 宿主）已装 **Node v24 于 `~/node24/bin`**（含 npm / npx），项目内 `npm install` 已装入 @commitlint/cli 19.8.1 + config-conventional 19.8.1。

```bash
# ~/node24/bin 不在默认 PATH：每次会话先 export，再执行 npm / npx / pre-commit 相关命令
export PATH=/home/azureuser/node24/bin:$PATH
npm install                     # 安装 @commitlint/cli 19.8.1 + config-conventional 19.8.1
pre-commit install --hook-type commit-msg
```

> **commitlint 钩子依赖 npx**：pre-commit 的 commitlint（以及 eslint / prettier）钩子 `entry` 均为 `npx --no-install ...`，需要 `npx` 在 PATH 上。默认 shell 不带 `~/node24/bin`，**直接 `git commit` 会因找不到 npx 而失败**；提交或手动跑钩子时需加前缀：
> `PATH=/home/azureuser/node24/bin:$PATH git commit ...`（或先 `export PATH`）。

## 7. 浏览器（Selenium 爬虫用，用到再下）

`tools/anjuke_gd_selenium_poc.py` 需本机真实浏览器；Selenium 4.6+ 用 selenium-manager 自动取 driver，无需手动下 chromedriver。
下载：[Chrome](https://www.google.com/chrome/) 或 [Chromium](https://www.chromium.org/)（Linux 可 `apt install chromium-browser`）。

## 8. 验证清单（建完环境后）

```bash
conda activate spark                        # 或 source .venv/bin/activate
python -c "import pyspark; print(pyspark.__version__)"   # 4.2.0
java -version                                              # 17.x
python docs/poc/data-foundation/avm_poc.py                # H1 论证结论
python docs/poc/core-prototype/mvp_prototype.py            # 8/8 验收
pre-commit run --all-files                                 # 提交前校验
```

## 9. 常见问题

- **`conda: command not found`**：conda 未在默认 PATH；先 `source ~/miniforge/etc/profile.d/conda.sh` 再 `conda activate spark`（全新机器按第 2 节装 Miniforge）。
- **`pyspark==4.2.0` 找不到**：PyPI 未发布时放宽版本约束为 `pyspark`。
- **PySpark 报找不到 Java**：设 `JAVA_HOME` 指向 Java 17，并确认 `java -version` 为 17.x。
- **`git commit` 报 `npx: command not found` / commitlint 钩子失败**：`~/node24/bin` 不在默认 PATH；提交前 `export PATH=/home/azureuser/node24/bin:$PATH` 或用 `PATH=/home/azureuser/node24/bin:$PATH git commit ...`。
- **`docker: permission denied`**：azureuser 在 docker group 但非登录 shell 不重读组变更；执行 `newgrp docker` 后重试，或直接 `sudo docker ...`。
- **沙箱限速无法建环境**：出网被限流时大文件（Miniforge/PySpark）下载极慢；请在有正常网络的机器执行。
