# 依赖清单：在 conda env `spark` 中安装
#
#   conda create -n spark python=3.10 -y
#   conda activate spark
#   conda install -n spark openjdk=17 -y     # PySpark 运行期需系统 Java 17
#   pip install -r requirements.txt
#
# 版本依据桌面参考文档《spark42_环境版本汇总.md》：
#   Conda env 仅列 Python 3.10.18 + PySpark 4.2.0（pip 来源）。

# —— 参考配置指定 ——
pyspark==4.2.0

# —— 本仓库运行 / 工具链所需 ——
aiohttp
beautifulsoup4
selenium
pre-commit==4.6.1
pytest
# 分布式层测试用的 Redis 替身：tools/anjuke_crawler/tests/test_distributed.py 在模块级
# import，缺失会让 `pytest tools/` 直接收集失败；orchestrator 的 conftest 也用它在无
# 真实 Redis 时降级。
fakeredis
