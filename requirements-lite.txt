# 轻量版依赖：仅用于跑通现有 PoC，跳过重型 Spark 技术栈（PySpark / Hadoop / Hive / Spark）
#
# 覆盖：
#   - docs/poc/* 两个 PoC（零依赖，其实无需安装）
#   - tools/anjuke_gd_poc.py            （异步爬虫，需 aiohttp + beautifulsoup4）
#   - tools/anjuke_gd_selenium_poc.py  （需 selenium + 本机 Chrome/Chromium）
#   - 提交前/提交信息钩子（pre-commit）、阶段 5 计划的 pytest 单测
#
# 安装（任选其一）：
#   pip install -r requirements-lite.txt
#   或：conda activate spark && pip install -r requirements-lite.txt
#
# 注意：不含 PySpark；需要 L2/L3 真实 Sedona/PySpark 时请用 environment.yml。

aiohttp
beautifulsoup4
selenium
pre-commit==4.6.1
pytest
